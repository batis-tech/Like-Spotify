export class UsersData {
 //same code in recipe model type-script just on shorter way
  constructor(public firstName: string, public lastName: string, public email: string, public password: string){}
}
